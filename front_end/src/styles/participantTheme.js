import { createMuiTheme } from '@material-ui/core';

export const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#F9A826',
    },
    secondary: {
      main: '#6C63FF',
    },
    success: {
      main: '#428a04',
    },
    background: {
      default: '#E6E6E6',
    },
  },
});
